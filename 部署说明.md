# 部署指南：《激活一下，实习生！》

## 推荐方案对比

| 方案 | 费用 | 国内访问速度 | 难度 | 推荐度 |
|------|------|-------------|------|:------:|
| **Railway** | 每月 $5-10 免费额度 | 中等 | ⭐ 简单 | ⭐⭐⭐⭐⭐ |
| **Render** | 免费额度 | 中等偏慢 | ⭐ 简单 | ⭐⭐⭐⭐ |
| **阿里云 函数计算** | 几乎免费（按量计费） | 快 | ⭐⭐ 中等 | ⭐⭐⭐⭐ |
| **Vercel + 后端分离** | 免费 | 快（前端）/ 中（后端） | ⭐⭐⭐ 稍复杂 | ⭐⭐⭐ |

**我的推荐**：先用 **Railway**，最快 10 分钟上线，零成本。

---

## 方案一：Railway（推荐，最快最省心）

### 第一步：把代码推送到 GitHub

```bash
# 1. 在项目目录初始化 git
cd "E:/A思考 就业 创业/实习/商赛/ai_intern_game_deepseek_version"

# 2. 创建 .gitignore（排除敏感文件）
echo "node_modules/
.env
.DS_Store" > .gitignore

# 3. 初始化并提交
git init
git add .
git commit -m "init: 激活一下，实习生！完整项目"

# 4. 在 GitHub 上新建一个仓库（不要勾选 README），然后推送
# 替换下面的 <用户名> 和 <仓库名>
git remote add origin https://github.com/<你的用户名>/<仓库名>.git
git push -u origin main
```

> ⚠️ 推代码前确保 `.env` 已在 `.gitignore` 中，否则 API Key 会泄露。

### 第二步：在 Railway 部署

1. 打开 https://railway.app ，用 GitHub 账号登录
2. 点击 **New Project** → **Deploy from GitHub repo**
3. 授权后选择你刚推送的仓库
4. Railway 会自动检测到 `package.json`，用 Node.js 启动

### 第三步：配置环境变量

在 Railway 项目面板中：
1. 进入项目 → **Variables** 选项卡
2. 添加以下变量：

```
PORT=3000
AI_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-f1a5bb31e61643fbaf0a2ff7d142ba9e
DEEPSEEK_MODEL=deepseek-v4-flash
DEEPSEEK_BASE_URL=https://api.deepseek.com
```

3. Railway 会自动重新部署。部署完成后，点击 **Settings** → **Domains** → **Generate Domain** 生成公网访问地址。

### 第四步：访问验证

打开 Railway 给你的 `https://xxx.railway.app`，你应该能看到游戏首页。跑完整流程测试 AI 功能是否正常。

---

## 方案二：Render（备选，免费但稍慢）

1. 打开 https://render.com ，用 GitHub 登录
2. 点击 **New +** → **Web Service**
3. 连接你的 GitHub 仓库
4. 填写配置：

| 字段 | 值 |
|------|-----|
| Name | `ai-intern-game` |
| Environment | `Node` |
| Build Command | `npm install` |
| Start Command | `node server.js` |
| Plan | **Free** |

5. 在 **Environment Variables** 中添加：
   - `DEEPSEEK_API_KEY` = 你的 Key
   - `AI_PROVIDER` = `deepseek`
   - `DEEPSEEK_MODEL` = `deepseek-v4-flash`
   - `DEEPSEEK_BASE_URL` = `https://api.deepseek.com`

6. 点击 **Create Web Service**
7. 等待部署完成（约 2-3 分钟），访问 `https://ai-intern-game.onrender.com`

> ⚠️ Render 免费版在 15 分钟无访问后会休眠，再次访问需要等待 30 秒左右唤醒。如果用于比赛展示，建议赛前半小时访问一次"预热"。

---

## 方案三：阿里云函数计算（国内访问最快的方案）

如果你需要国内评委能流畅访问，这是最佳方案。前提是你有阿里云账号。

### 1. 安装依赖并打包

```bash
# 安装依赖
npm install --production

# 将以下文件打包为 zip：
# - server.js
# - index.html
# - package.json
# - node_modules/
# - assets/
```

### 2. 阿里云函数计算配置

1. 登录阿里云控制台 → 函数计算 FC
2. 创建函数 → **使用自定义运行时** → **Node.js 20**
3. 上传 zip 包
4. **函数入口**：`server.handler`
5. **环境变量**：同样配置 DEEPSEEK_API_KEY 等
6. 创建成功后，绑定**自定义域名**（函数计算会提供一个默认测试域名）

> ⚠️ 注意：Serverless 环境下需要修改 server.js 中的监听方式。这一步比 Railway 复杂，如果你不熟悉 Serverless，建议优先用 Railway。

---

## 部署后需要确认的事

| 检查项 | 怎么确认 |
|--------|---------|
| ✅ 游戏页面正常打开 | 访问首页看到开始界面 |
| ✅ 剧情能正常推进 | 点"开始剧情"，对话和选择正常 |
| ✅ 结局页正常展示 | 走完流程看到结局和总结 |
| ✅ AI 报告正常生成 | 结局页看到"AI 个性化职场能力报告"卡片 |
| ✅ AI 返回结构化数据 | 浏览器 F12 → Network → 看 `/api/ai-review` 的 Response 有 `review` 字段 |

---

## 常见问题

**Q：部署后 AI 接口返回 500 错误？**
A：检查环境变量 `DEEPSEEK_API_KEY` 是否已正确设置，是否是有效的 API Key。

**Q：图片不显示？**
A：确认 `assets/` 文件夹已包含在 git 提交中，路径在代码中使用的是相对路径。

**Q：Railway 域名被墙？**
A：如果用于国内评委展示且 Railway 域名打不开，可以用方案一先把服务跑通用于自测，赛前再迁移到阿里云。

**Q：部署后访问很慢？**
A：Railway 和 Render 的服务器在海外，国内访问会有延迟。如果需要国内极速访问，使用方案三（阿里云）。
